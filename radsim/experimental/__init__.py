"""Experimental RadSim modules.

These modules are available for direct imports and tests, but they are not part
of the main CLI execution path. They are kept under an explicit experimental
namespace so their status is visible without breaking compatibility for the
legacy import paths.
"""
