"""User-added custom tools.

This file is auto-generated and appended to by the `add_tool` meta-tool.
Each entry registers a new tool that the model can call by name.

Edit by hand at your own risk — `add_tool` will append, not merge.
"""

from typing import Any

# Map of tool_name -> callable(tool_input: dict) -> dict
CUSTOM_REGISTRY: dict[str, Any] = {}

# List of Anthropic-style tool definitions for CUSTOM_REGISTRY entries
CUSTOM_DEFINITIONS: list[dict] = []
