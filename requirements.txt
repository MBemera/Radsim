# Core dependencies - PINNED for production stability
anthropic==0.84.0

# Optional providers - PINNED versions
openai==1.58.0
google-genai==0.8.0
playwright==1.58.0

# Development - PINNED versions
pytest==9.0.2
ruff==0.15.4

# Note: Run `pip freeze > requirements.txt` before deployment
# to capture exact versions from your environment
